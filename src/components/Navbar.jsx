function Navbar() {
  return (
    <div
      style={{
        padding: "15px",
        background: "#111",
        color: "#fff",
        position: "fixed",
        top: 0,
        left: 0,
        width: "100%",
        zIndex: 1000,
      }}
    >
      <h3>LMS Dashboard</h3>
    </div>
  );
}

export default Navbar;
